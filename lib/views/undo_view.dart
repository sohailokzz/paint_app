import 'package:flutter/material.dart';

class UndoView extends StatelessWidget {
  const UndoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Undo",
      ),
    );
  }
}
